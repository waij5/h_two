define(['jquery', 'bootstrap', 'front'], function($, undefined) {
    var Controller = {
        index: function() {
            // Frontend.login('admin', '123456');
        }
    };

    return Controller;
});